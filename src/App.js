import React from 'react';
import './App.css';

function App() {
    return (
        //BEM naming convention
        <
        div className = "App" >
        <
        h1 > Lets build a Whatsapp Clone! < /h1>   <
        div className = "app_body" > { /*side bar*/ } { /*Chat */ } <
        /div>   <
        /div>
    );
}

export default App;